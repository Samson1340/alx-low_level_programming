#include "main.h"
#include <stdio.h>

/**
* reverse_array - reverse array
* @a: the pointer
* @n: number of byte
* Return: the copied string
*/

void reverse_array(int *a, int n)
{
int i;
for (i = n - 1; i >= 0; i--)
{
return (a[i]);
}
