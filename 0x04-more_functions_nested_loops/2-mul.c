#include "main.h"

/**
* mul - A function that multiplies two no
* @a: the first number
* @b: the second number
* Return: the result of the multiplication
*/

int mul(int a, int b)
{
return (a * b);
}
